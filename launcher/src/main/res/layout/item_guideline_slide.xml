<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:overScrollMode="never">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingHorizontal="28dp"
        android:paddingTop="20dp"
        android:paddingBottom="24dp">

        <!-- Icon Container -->
        <FrameLayout
            android:layout_width="110dp"
            android:layout_height="110dp"
            android:layout_marginBottom="24dp"
            android:background="@drawable/bg_guideline_icon_circle">

            <ImageView
                android:id="@+id/iv_guideline_icon"
                android:layout_width="54dp"
                android:layout_height="54dp"
                android:layout_gravity="center"
                android:contentDescription="@null"
                android:src="@drawable/ic_camera"
                app:tint="@color/secondary_teal_dark" />
        </FrameLayout>

        <!-- Step Tag Badge -->
        <TextView
            android:id="@+id/tv_guideline_step_tag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:background="@drawable/bg_guideline_badge"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.08"
            android:paddingHorizontal="14dp"
            android:paddingVertical="5dp"
            android:text="STEP 1 OF 4 • CAMERA SCAN"
            android:textColor="@color/secondary_teal_dark"
            android:textSize="11sp" />

        <!-- Title -->
        <TextView
            android:id="@+id/tv_guideline_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:gravity="center"
            android:text="Scan Your Face"
            android:textColor="@color/text_primary"
            android:textSize="24sp" />

        <!-- Subtitle -->
        <TextView
            android:id="@+id/tv_guideline_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="16dp"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:text="AI Facial Proportions &amp; Shape Detection"
            android:textColor="@color/secondary_teal_dark"
            android:textSize="13sp" />

        <!-- Description -->
        <TextView
            android:id="@+id/tv_guideline_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="20dp"
            android:fontFamily="sans-serif"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:text="Tap the camera button on the bottom navigation bar to capture or upload your photo. Our biometric AI evaluates your face proportions, cheekbones, and jawline in real-time."
            android:textColor="@color/text_secondary"
            android:textSize="14.5sp" />

        <!-- Tip Box -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_guideline_tip_card"
            android:orientation="horizontal"
            android:padding="14dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginTop="1dp"
                android:layout_marginEnd="10dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_lightbulb"
                app:tint="@color/secondary_teal_dark" />

            <TextView
                android:id="@+id/tv_guideline_tip"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="sans-serif"
                android:lineSpacingExtra="2dp"
                android:text="Position your face inside the oval reticle in good lighting for the most accurate facial classification."
                android:textColor="@color/text_secondary"
                android:textSize="13sp" />
        </LinearLayout>

    </LinearLayout>
</androidx.core.widget.NestedScrollView>
